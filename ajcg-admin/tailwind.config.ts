// This file kept as a stub for IDE type inference.
// The active config is `tailwind.config.js` (CommonJS) which Next.js loads first.
import type { Config } from "tailwindcss";
const config: Config = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
};
export default config;
